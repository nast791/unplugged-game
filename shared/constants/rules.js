/** Дефолтные правила UnPlugged (не в state). */
export const rules = {
  actionsPerTurn: 2,
  openingHand: 5,
  maxHandSize: 7,
  exhaustionDamage: 2,
  canPassThroughTeammates: true,
  canPassThroughEnemies: false,
  maxPlayers: 4,
  minPlayers: 2,
};
